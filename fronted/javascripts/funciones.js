
/*
document.addEventListener("DOMContentLoaded", () => {
  const burger = document.getElementById('burguer');
  const menu = document.getElementById('nav-menu');
  
  const btn = document.getElementById("botondeplegable");
  const form = document.querySelector(".actualizar");

  //validación del botón desplegable
  if (btn && form) {
    btn.addEventListener("click", () => {
      form.classList.toggle("active");
    });
  }

  //validación del menú hamburguesa
  if (burger && menu) {
    burger.addEventListener('click', () => {
      burger.classList.toggle('active');
      menu.classList.toggle('active');
    });
  }
});
*/